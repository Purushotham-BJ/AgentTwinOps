# Config

This directory is reserved for the config concern in the backend application.

## Responsibility

- Contain the config modules for the future implementation.
- Preserve architecture boundaries and keep the package intentionally lightweight.
- Avoid business logic until the implementation phase begins.
